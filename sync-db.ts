import { AppDataSource } from './src/data/postgres/data-source'; AppDataSource.initialize().then(() => { console.log('Sincronizando...'); return AppDataSource.synchronize(false); }).then(() => { console.log('Sincronizacion completa'); process.exit(0); }).catch((err: any) => { console.error(err); process.exit(1); });
